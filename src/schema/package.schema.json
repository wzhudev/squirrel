{
    "$schema": "http://json-schema.org/schema",
    "allOf": [
        {
            "$ref": "http://json.schemastore.org/package",
            "properties": {
                "squirrel": {
                    "title": "SquirrelPackageConfig",
                    "description": "JSON schema for squirrel config extending package.json",
                    "properties": {
                        "outDir": {
                            "description": "Destination of productions.",
                            "type": "string",
                            "default": "publish"
                        }
                    }
                }
            }
        }
    ]
}